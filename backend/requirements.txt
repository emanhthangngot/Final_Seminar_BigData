fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic>=2.7.0
python-multipart>=0.0.9
pandas>=2.0.0

# Re-use all dependencies from ../requirements.txt
# (run: pip install -r ../requirements.txt -r requirements.txt)
